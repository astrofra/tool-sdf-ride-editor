`app/sdf-output/` is the official drop zone for meshes and textures generated from `app/sdf-scenes/`.

Typical contents include `.obj`, normal maps, AO maps, and packed surface textures emitted by the SDF pipeline before later import into HARFANG assets.
